--Simple if else statements that has been occured in this scenarios
select s.user_id, round(avg(if(c.action like 'confirmed',1,0)),2) 'confirmation_rate' from signups s left join confirmations c on s.user_id = c.user_id group by user_id;